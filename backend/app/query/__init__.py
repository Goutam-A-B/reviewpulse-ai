"""Phase 3 — Query Tools: the deterministic evidence layer (Quote Retriever + Stats Engine).

These are tools, not agents: pure retrieval and plain arithmetic, reproducible,
exposed behind stable contracts for the Analyst Agent (Phase 4) to call.
"""
